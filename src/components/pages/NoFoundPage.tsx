import React from 'react';

// /. imports

const NoFoundPage: React.FC = () => {
    return (
        <h2 className="section-title">
            <i> Sorry, this page is not exist</i>
        </h2>
    );
};

export default NoFoundPage;
